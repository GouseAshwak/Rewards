const {earnPoints } = require('./earnPoints')
const {createUser} =require('./createUser')
const {redeemPoints} =require('./redeemPoints')
const{transaction} = require('./transaction')

module.exports = {earnPoints,createUser,redeemPoints,transaction}

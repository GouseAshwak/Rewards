const {validateEarnPoints } = require('./validateEarnPoints')
const {validateRedeemPoints} =require('./validateRedeemPoints')
const {validateTransaction} =require('./validateTransaction')

module.exports = {validateEarnPoints,validateRedeemPoints,validateTransaction}